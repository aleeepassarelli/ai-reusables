# -----------------------------------------------------------------
# 🧩 Atomic Architecture - Requisitos de Ambiente (v1.1 - Stack)
# -----------------------------------------------------------------

# --- 1. Núcleo, APIs e Validação ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.0
jsonschema==4.23.0
httpx==0.27.0           # (Substitui 'requests' para I/O assíncrono)
requests==2.32.3
gunicorn==23.0.0          # Para produção (opcional no dev local)

# --- 2. Segurança ---
python-jose==3.3.0
PyJWT==2.9.0
cryptography==43.0.0

# --- 3. Memória (Camada 1: Atoms) ---
graphiti-core[google-genai,falkordb]==0.17.0 # Motor do grafo
zep-python==1.8.0                       # Memória conversacional
neo4j==5.26.0                           # Driver do banco de dados de grafo
asyncpg==0.29.0                         # Driver Postgres (para Zep/Graphiti)
psycopg2-binary==2.9.9                  # Fallback driver Postgres
redis==5.2.0                            # Cache de sessão e filas
qdrant-client==1.12.0                   # Banco de dados vetorial

# --- 4. Adapters & Plugins (Camada 3: Organisms) ---
openai==1.54.0                          # Essencial: Para OpenAI e compatibilidade (Ollama/Jan)
google-generativeai==0.8.0
anthropic==0.39.0
dashscope==1.18.0                       # SDK para Qwen (agent_vision)
importlib-metadata==8.5.0
docker==7.1.0

# --- 5. Dashboard API & Observabilidade ---
orjson==3.10.7                          # Serializador JSON rápido para FastAPI
watchfiles==0.24.0                      # Para hot-reloading (Uvicorn)
prometheus-client==0.21.0               # Métricas (Grafana)
