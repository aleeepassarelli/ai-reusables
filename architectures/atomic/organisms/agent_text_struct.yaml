# -----------------------------------------------------------------
# 🧩 Organismo: agent_text_struct.yaml
# -----------------------------------------------------------------
agent_id: "agent_text_struct"
name: "Agente de Estrutura (O Organizador)"
description: "Converte texto não estruturado em JSON/YAML formatado."

# --- Tipo de Execução ---
type: "llm_chat"

# --- Configuração do LLM (Ollama) ---
llm_config:
  # Cérebro: GPT-OSS 20B (via Ollama)
  model: "gpt-oss:20b" # (Supondo que este modelo esteja no Ollama)
  
  # Missão (Prompt do Sistema)
  system_prompt: |
    Você é um especialista em análise semântica e estruturação de dados.
    Sua única missão é converter texto não estruturado em formato JSON
    ou YAML, seguindo EXATAMENTE o 'prompt' do usuário.
    Sua resposta deve conter APENAS o bloco JSON/YAML formatado.
    Não adicione nenhuma outra palavra ou explicação.
